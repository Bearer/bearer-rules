high:
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 3
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 3
      snippet: Net::HTTP.post_form("http://#{params[:oops]}/users/search")
      fingerprint: fa7acece587f08a4750ef1009d677b71_0
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 5
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 5
      snippet: |-
        Net::HTTP.start(params[:host]) do |instance1|
          instance1.head(params[:path])
        end
      fingerprint: fa7acece587f08a4750ef1009d677b71_1
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 6
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 6
      snippet: instance1.head(params[:path])
      fingerprint: fa7acece587f08a4750ef1009d677b71_2
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 9
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 9
      snippet: 'Net::HTTP::Get.new(params[:oops], { "X-Test": 42 })'
      fingerprint: fa7acece587f08a4750ef1009d677b71_3
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 11
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 11
      snippet: Net::HTTP.start(params[:oops])
      fingerprint: fa7acece587f08a4750ef1009d677b71_4
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 12
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 12
      snippet: instance2.ipaddr = request.env[:oops]
      fingerprint: fa7acece587f08a4750ef1009d677b71_5
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 13
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 13
      snippet: instance2.send_request("GET", params[:oops], nil)
      fingerprint: fa7acece587f08a4750ef1009d677b71_6
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 15
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 15
      snippet: Net::HTTP.new(params[:oops])
      fingerprint: fa7acece587f08a4750ef1009d677b71_7
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 16
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 16
      snippet: instance3.patch(params[:path])
      fingerprint: fa7acece587f08a4750ef1009d677b71_8
    - rule:
        cwe_ids:
            - "918"
        id: ruby_lang_http_url_using_user_input
        title: HTTP communication with user-controlled destination detected.
        description: |
            ## Description

            Applications should not connect to locations formed from user input.
            This rule checks for URLs containing user-supplied data.

            ## Remediations

            ❌ Avoid using user input in HTTP URLs:

            ```ruby
            Faraday.get("https://#{params[:host]}')
            ```

            ✅ Use user input indirectly to form a URL:

            ```ruby
            host =
              case params[:host]
              when "option1"
                "api1.com"
              when "option2"
                "api2.com"
              end

            Faraday.get("https://#{host}')
            ```
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_http_url_using_user_input
      line_number: 18
      filename: /tmp/scan/unsafe_net_http.rb
      parent_line_number: 18
      snippet: instance4.post(request.env[:oops])
      fingerprint: fa7acece587f08a4750ef1009d677b71_9

