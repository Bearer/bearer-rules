console.error(`Value: %s`, req.params.value, e)

util.format("Value: %s", req.params.value)

util.formatWithOptions({}, 'Value: %s %s', req.params.value, x)

console.error(`Value: ${req.params.value ? ok : ok}`, e)
