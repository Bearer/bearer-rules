import java.sql.*;

public class SQLExample {
  public static void main(String tableName) {
    final String staticStr = "SELECT * FROM " + tableName
    String sqlQuery = String.format(staticStr);
  }
}