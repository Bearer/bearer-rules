{
  "scripts": {
    "test": "jest",
    "generate_rule": "node scripts/generate_rule.js",
    "bulk-update": "node scripts/bulk_update.js",
    "validate-rule-urls": "node scripts/validate_rule_urls.js"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "prettier": "^3.2.4"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "csv-parse": "^5.5.3",
    "yaml": "^2.3.4"
  }
}
